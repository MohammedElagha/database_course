# Indexing

# 4 types: primary, index, unique, fulltext

# index
CREATE INDEX index_name ON table_name (col_list);

# unique
CREATE UNIQUE INDEX index_name ON table_name (col_list);

# fulltext
CREATE FULLTEXT INDEX index_name ON table_name (col_list);